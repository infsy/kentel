{# src/Nurun/RhBundle/Resources/views/ConseillerCertification/delete.html.twig #}

{% extends "NurunRhBundle::layout.html.twig" %}

{% block title %}
  Supprimer une certification - {{ parent() }}
{% endblock %}

{% block body %}

  <h2>Supprimer une certification</h2>

  <p>
    Etes-vous certain de vouloir supprimer cette certification "{{ conseillerCertification.certification.acronyme }}"
    pour {{conseillerCompetence.conseiller.prenom }} {{conseillerCompetence.conseiller.nom }} ?
  </p>

  {# On met l'id de l'annonce dans la route de l'action du formulaire #}
  <form action="{{ path('nurun_conseiller_certification_delete', {'id': conseillerCertification.id}) }}" method="post">
    <a href="{{ path('nurun_conseiller_certification_index', {'id': conseillerCertification.conseiller.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour
    </a>
    {# Ici j'ai écrit le bouton de soumission à la main #}
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {# Ceci va générer le champ CSRF #}
    {{ form_rest(form) }}
  </form>

{% endblock %}
